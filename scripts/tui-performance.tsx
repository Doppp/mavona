import {testRender} from '@opentui/solid';import {createSignal} from 'solid-js';import {cpus,totalmem} from 'node:os';import {Screen,type ScreenModel} from '../packages/tui/screen';
const messages=Array.from({length:50000},(_,i)=>({id:'message-'+i,text:`Transcript line ${i}: 注文 Keep the accepted Rails task and independent evidence visible.`}));const [model,setModel]=createSignal<ScreenModel>({repository:'performance',status:'ready',draft:'',messages,source:null});const started=performance.now();const setup=await testRender(()=> <Screen model={model} actions={{draft:text=>setModel(m=>({...m,draft:text})),async submit(){},closeSource(){},close(){}}}/>,{width:80,height:24});
try{await setup.renderOnce();const initialMs=performance.now()-started;const samples:number[]=[];for(let i=0;i<20;i++){const before=performance.now();setModel(m=>({...m,messages:[...m.messages,{id:'stream-'+i,text:'Stream delta '+i}]}));setup.mockInput.pressKey('x');await setup.renderOnce();samples.push(performance.now()-before);}samples.sort((a,b)=>a-b);if(model().draft!=='x'.repeat(20))throw new Error('Composer input lost');console.log(JSON.stringify({hardware:cpus()[0]?.model,memoryBytes:totalmem(),runtime:Bun.version,platform:process.platform,architecture:process.arch,fixtureMessages:messages.length,dimensions:'80x24',initialMs,eventToFrameP95Ms:samples[Math.ceil(samples.length*.95)-1],rssBytes:process.memoryUsage().rss,composer:'preserved'}));}
finally{setup.renderer.destroy();}
