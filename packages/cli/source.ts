import {parseArguments,single} from './options';import {readCommittedSource} from '../tools/source-history';import {readSource,selectLines} from '../tools/source';
export async function sourceCommand(raw:string[]){const args=parseArguments(raw,['root','path','revision','start','end'],[]);if(args.positionals.length)throw new Error('Use source --path PATH [--revision HEAD|COMMIT]');const root=single(args,'root',process.cwd())!,path=single(args,'path');if(!path)throw new Error('Source path required');const revision=single(args,'revision','worktree')!;const source=revision==='worktree'?await readSource(root,path):await readCommittedSource(root,path,revision);if(args.values.has('start')||args.values.has('end')){const start=Number(single(args,'start','1')),end=Number(single(args,'end',String(start)));console.log(JSON.stringify({schemaVersion:1,...selectLines(source,start,end),scope:'read-only source evidence',correctness:'unknown'}));}else console.log(JSON.stringify({schemaVersion:1,...source,scope:'read-only source evidence',correctness:'unknown'}));return 0;}
