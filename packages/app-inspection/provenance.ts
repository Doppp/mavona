import {captureRepositoryState} from '../tools/repository-state';
export interface EvidenceContext {sessionId:string|null;taskId:string|null;repository:{commit:string|null;dirtyDigest:string|null;status:'passed'|'unknown';sampledAt:string|null};server:{origin:string|null;environment:'development'|'test'|null;fixtureRevision:string|null}}
export function unknownEvidenceContext():EvidenceContext{return {sessionId:null,taskId:null,repository:{commit:null,dirtyDigest:null,status:'unknown',sampledAt:null},server:{origin:null,environment:null,fixtureRevision:null}};}
export async function repositoryEvidence(root:string,sessionId:string,taskId?:string):Promise<EvidenceContext>{const context=unknownEvidenceContext();context.sessionId=sessionId;context.taskId=taskId??null;try{const state=await captureRepositoryState(root);context.repository={commit:state.head,dirtyDigest:state.status==='passed'?state.digest:null,status:state.status,sampledAt:new Date().toISOString()};}catch{context.repository.sampledAt=new Date().toISOString();}return context;}
